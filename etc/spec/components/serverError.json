{
    "description": "Error Response, returned when an unexpected error or a setup issue in the API server is detected.",
    "content": {
        "application/json": {
            "schema": {"$ref": "../schema/Error.json"}
        }
    }
}

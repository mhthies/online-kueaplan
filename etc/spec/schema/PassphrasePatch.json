{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "https://kuaplan.de/meta/schema/PassphrasePatch.json",
    "title": "Authentication PassphrasePatchset",
    "description": "A patch set of for updating a Passphrase (authentication token)",
    "type": "object",

    "properties": {
        "comment": {
            "description": "An internal comment, only visible to the admins. It can be used to document the purpose of the passphrase, when multiple passphrases for the same role are used.",
            "type": "string"
        },
        "validFrom": {
            "description": "If present and not null, the passphrase is only valid after this point in time.",
            "type": "string",
            "format": "date-time",
            "nullable": true
        },
        "validUntil": {
            "description": "If present and not null, the passphrase is only valid before this point in time. Afterwards, uses can no longer authenticate with this passphrase and existing sessions will lose the privileges granted by this passphrase. Users are informed about the expired passphrase by a specific error message. To revoke privileges in a graceful way, this can be set to a point in the past.",
            "type": "string",
            "format": "date-time",
            "nullable": true
        }
    }
}
